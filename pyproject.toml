[project]
name = "movex-domain"
version = "0.1.0"
description = "Shared domain model and business logic for MoveX services."
readme = "README.md"
authors = [
    { name = "Dumbrovskii", email = "dumbrovskyy@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
